class Solution:
    def plusOne(self, digits: List[int]) -> List[int]:
        if digits==[]:
            return [1]
        num=""
        for i in digits:
            num += str(i)
        num=int(num)+1
        num=str(num)
        a=[]
        for i in num:
            a.append(i)
        return a
